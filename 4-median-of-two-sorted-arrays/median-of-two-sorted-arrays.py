class Solution:
    def findMedianSortedArrays(self, nums1: List[int], nums2: List[int]) -> float:
        # Xlist = sorted(nums1 + nums2)
        # median = 0
        # if len(Xlist)%2 == 1:
        return median(sorted(nums1 + nums2))
        #Was?
